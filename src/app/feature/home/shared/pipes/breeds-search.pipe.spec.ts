import { BreedsSearchPipe } from './breeds-search.pipe';

describe('BreedsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BreedsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
